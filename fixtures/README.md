# Test Fixtures

Place real music files here for integration testing.

| File | Purpose |
|------|---------|
| `simple-4-4.mid` | Basic MIDI, 4/4, 1 piano track, 8 bars |
| `alto-sax.mid` | MIDI with program 65 (Alto Sax), 1 track |
| `with-repeats.gp5` | GP5 with `||:` `:||` repeat signs |
| `multitrack.gpx` | GPX: guitar + bass + sax |

These files are **not committed** (binary, copyright-sensitive).
Generate them with MuseScore, Guitar Pro, or any MIDI sequencer.
